services:
  - type: web
    name: fusion-motors-demo
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ANALYTICS_ADMIN_KEY
        generateValue: true
      - key: IP_HASH_SALT
        generateValue: true
